// import frmData from 'pg-format';
// const postgre = require('@Connection/Postgre');

export class profile{
    // format?: any;
    // table: string;
    constructor(){
        // this.format = {
        //     format: (data: string) => {
        //         console.log(frmData(data));
        //         return frmData(data);
        //     }
        // }
        // this.table = 'profile';
    }

    // getAllData(limit: number = 0){
    //     // let query = `SELECT * FROM ${this.table} LIMIT ${limit === 0 ? 99999 : limit}`;
    //     // let result = postgre.query(query);

    // }
}