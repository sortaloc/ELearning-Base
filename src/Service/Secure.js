const SecureController = require('@Controllers/SecureController');

module.exports = (router) => {

    router.post('/createUserPin', async (req, res) => {
        // Simple Hashing
        // Create IDCustomer
        // Create PIN
        // Generate Secret JWT
    })

    return router;
}