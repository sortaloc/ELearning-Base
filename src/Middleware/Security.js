// import { Request, Response, NextFunction } from "express";

// Security Headers and raw Data Function

// Flow security
// Bisa pake headers
// Dan pake di body

module.exports =  MiddlewareValidation = (req, res, next) => {
    console.log(req.headers)
    
    next();
}