namespace SecureController {
    const database = require('@Model/index');
    const { STRUCTURE } = require('@Config/Config');
    
    export class SecureController {
        generateKey = (pin: string) => {
    
        }
    }
    
    // module.exports = new SecureController;
}
